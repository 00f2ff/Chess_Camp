<% unless @family_students.empty? %>
	<h3>&nbsp;</h3>
	<div class="panel clearfix">
		<h5>Students</h5>
		<ul class="side-nav">
			<% @family_students.each do |student| %>
				<li><%= link_to "#{student.name}", student_path(student) %></li>
			<% end %>
		</ul>
	</div>
<% else %>
	<div class="panel clearfix">
		<h5><em>This family has no students</em></h5>
	</div>
<% end %>
	