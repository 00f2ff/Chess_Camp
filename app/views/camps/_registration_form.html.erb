<%= simple_form_for(@registration, html: { class: 'form-horizontal' }) do |f| %>
	<fieldset>
		<legend><%= controller.action_name.capitalize %> Registration</legend>
		<%= f.input :camp_id, as: :hidden, input_html: { value: @camp.id } %>
		<!-- how do we check the student is allowed to register? <%= #f.input :student_id, collection: Student. %> -->
		