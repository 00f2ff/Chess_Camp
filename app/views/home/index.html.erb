<% if current_user && current_user.role == 'admin' %>
	<%= render partial: 'admin_dash' %>
<% elsif current_user && current_user.role == 'instructor' %>
	<%= render partial: 'instructor_dash' %>
<% else %>
	<%= render partial: 'guest_dash' %>
<% end %>
